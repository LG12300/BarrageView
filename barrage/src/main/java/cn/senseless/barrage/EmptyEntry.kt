package cn.senseless.barrage

import android.graphics.Paint
import android.graphics.RectF

class EmptyEntry(
    maxBounds: RectF,
    paint: Paint,
    bounds: RectF,
) : BaseEntry(maxBounds, paint, bounds)